# core/point.py

class Point:
    # Инициализатор класса Point, который создает новый экземпляр точки с указанными координатами, не используется
    def __init__(self, x, y):
        self.x = x
        self.y = y
